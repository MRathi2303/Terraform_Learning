variable "ami_value" {
   description = "There we store the ami_value for creating ec2 instances"
 }

 variable "keyValue" {
    description = "Here we refer to the key value pair"
 }

 variable "instances_Type" {
   description = "Here we define the type of instance we need to create"
 }
